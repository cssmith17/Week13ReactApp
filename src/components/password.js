import React from 'react';

export default class Password extends React.Component {
    render() {
        return (
            <input id="text-field" type="password" className="form-control" placeholder="Password"></input>
        )
    }
}